<catel:UserControl x:Class="Orc.FilterBuilder.AlternativeExample.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:catel="http://catel.codeplex.com"
             xmlns:controls="clr-namespace:Orc.FilterBuilder.AlternativeExample.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <controls:JsonCollectionViewer Source="{Binding OriginalCollection}" />
        <controls:JsonCollectionViewer Grid.Column="1" Source="{Binding FilteredCollection}" />
    </Grid>
</catel:UserControl>
